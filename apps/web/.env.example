# Only needed in production, when the frontend and backend are deployed to different
# domains (e.g. two separate Render services). Leave unset for local dev — the Vite dev
# server proxy (vite.config.ts) already forwards "/api" to the local backend.
VITE_API_URL=https://your-backend-domain.onrender.com/api/v1
